<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>io.github.shawndrash.pyprism</id>

    <!-- name, version, description, change-notes, idea-version are patched in from
         gradle.properties / README.md / CHANGELOG.md by the IntelliJ Platform Gradle Plugin. -->

    <vendor url="https://github.com/shawndrash/PyPrism">shawndrash</vendor>

    <!-- Plugin dependencies. The plugin requires the Python plugin (bundled in PyCharm Professional/Community). -->
    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.modules.python</depends>

    <resource-bundle>messages.PyPrismBundle</resource-bundle>

    <extensions defaultExtensionNs="com.intellij">
        <annotator
            language="Python"
            implementationClass="io.github.shawndrash.pyprism.annotator.PythonEnhancedAnnotator"/>
        <colorSettingsPage
            implementation="io.github.shawndrash.pyprism.colors.PythonEnhancedColorSettingsPage"/>
        <!-- Out-of-the-box default colours per base scheme. Users can still override
             these in Settings → Editor → Color Scheme → Python. Without these,
             PYTHON_ENHANCED_CLASS_REFERENCE falls back to CLASS_NAME, which is
             visually identical to the stock class name token on Classic Light. -->
        <additionalTextAttributes scheme="Default" file="colorSchemes/PyPrismDefault.xml"/>
        <additionalTextAttributes scheme="Darcula" file="colorSchemes/PyPrismDarcula.xml"/>
    </extensions>
</idea-plugin>
